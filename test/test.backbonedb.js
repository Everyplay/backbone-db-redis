/*var assert = require('assert');
var setup = require('./setup');
var MyModel = setup.MyModel;
var MyCollection = setup.MyCollection;
var shared = require('backbone-db/test');

describe('backbone-db', function () {
  // Run backbone-db tests on our model & collection.
  before(function () {
    this.Model = MyModel;
    this.Collection = MyCollection;
    this.db = this.Collection.prototype.db;
  });

  shared.shouldImplementDb(function () {

  });

  after(function (next) {
    setup.clearDb(next);
  });
});

*/
